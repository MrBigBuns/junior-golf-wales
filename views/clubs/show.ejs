<%- include('../partials/head', { pageTitle: club.name + ' — junior golf events' }) %>

<% if (club.has_course_photo_image) { %>
  <img src="/clubs/<%= club.id %>/course-photo-image" alt="<%= club.name %>" class="course-image" style="max-width:600px;">
<% } else if (club.course_image_url) { %>
  <img src="<%= club.course_image_url %>" alt="<%= club.name %>" class="course-image" style="max-width:600px;">
<% } %>

<div style="display:flex;align-items:center;gap:10px;">
  <% if (club.has_logo_image) { %><img src="/clubs/<%= club.id %>/logo-image" alt="<%= club.name %> logo" class="club-logo-inline"><% } %>
  <h1><%= club.name %></h1>
</div>
<p class="subtitle"><%= club.region %> Wales<% if (club.address) { %> · <%= club.address %><% } %></p>
<% if (club.website) { %><p><a href="<%= club.website %>" target="_blank" rel="noopener">Club website</a></p><% } %>
<% if (club.facebook_url || club.instagram_url || club.x_url) { %>
  <div class="social-links">
    <% if (club.facebook_url) { %><a href="<%= club.facebook_url %>" target="_blank" rel="noopener">Facebook</a><% } %>
    <% if (club.instagram_url) { %><a href="<%= club.instagram_url %>" target="_blank" rel="noopener">Instagram</a><% } %>
    <% if (club.x_url) { %><a href="<%= club.x_url %>" target="_blank" rel="noopener">X</a><% } %>
  </div>
<% } %>

<h2>Upcoming junior events</h2>
<% if (events.length === 0) { %>
  <p>No upcoming junior events listed for this club yet.</p>
<% } %>
<ul class="related-list">
  <% events.forEach(function(e) { %>
    <li><a href="/events/<%= e.slug %>"><%= e.title %> — <%= new Date(e.date_start).toLocaleDateString('en-GB') %></a></li>
  <% }); %>
</ul>

<%- include('../partials/foot') %>
