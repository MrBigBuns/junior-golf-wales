services:
  - type: web
    name: junior-golf-wales
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: ADMIN_USER
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: junior-golf-wales-db
          property: connectionString

databases:
  - name: junior-golf-wales-db
    plan: free
